Flask>=3,<4
flask-cors>=4,<7
PyJWT>=2.8,<3
waitress>=3,<4
google-auth>=2.29,<3
requests>=2.31,<3
python-dotenv>=1.0,<2

# Radar map (app.py embed()): numpy is required. UMAP is optional (falls back to PCA).
numpy>=1.26

# 音源解析 (vibe_analyzer.py) は任意。入れると実測BPM・雰囲気・mood が使えるようになる。
#   librosa + soundfile        … 実測BPM/ビート/雰囲気の必須依存 (pip install librosa soundfile)
#   torch / laion-clap ほか    … mood とテンポ感の判定 (任意・重い)
# 注意: laion-clap は「numpy<2」を要求するメタデータを持つが numpy 2 でも動作する。
#       numpy を下げると librosa/scipy が壊れるため --no-deps で入れること:
#   pip install --no-deps laion-clap
#   pip install torch torchvision torchlibrosa transformers ftfy
#   pip install regex pyyaml wcwidth progressbar2 webdataset wget h5py pandas
# 注意: pandas / webdataset / braceexpand は laion-clap が import 時に読み込むため削除できない
#       (pandas は librosa でも使う)。外すと import laion_clap が失敗して CLAP が無効になる。
#       .venv の大半 (1.8GB) は CLAP の重み 630k-audioset-best.pt である。
# yt-dlp + ffmpeg も必要 (音源の取得と WAV 変換)。

# .venv 軽量化メモ (2026-09: 全体 2.83GB -> 2.5GB、動作検証済み)
#   再作成時は venv を作り直した後に下記を適用すること (.venv は git 管理外のため)。
#   1. venv パッチ (sklearn 不要化):
#      laion_clap/clap_module/__init__.py の
#        from .loss import ClipLoss, gather_features, LPLoss, lp_gather_features, LPMetrics
#      を try/except ImportError で囲む (学習用 loss の import を遅延。推論は影響なし)。
#      適用後は scikit-learn を入れなくてよい (joblib は librosa が使うため残す)。
#      なお transformers/generation/candidate_generator.py は is_sklearn_available()
#      ガード済みのため無修正で動く。
#   2. 削除済み (再作成時は入れないか、入れた後に消す):
#      __pycache__ 全削除 / 各パッケージの tests/test/testing ディレクトリ /
#      transformers/models の bert,roberta,bart,gpt2,auto 以外の arch /
#      rich,typer,pygments,markdown-it,shellingham,mdurl,networkx,setuptools /
#      hf_xet / 数値計算に使わない .py (torch 未使用部・scipy 未使用部等。
#      起動時 import trace + 全テスト + CLAP実推論で検証)
#      削除禁止: 630k-audioset-best.pt / torch 本体・lib / numpy/testing・torch/testing
#      (import 時に必要) / librosa 全体 (lazy_loader のため) / pandas・PIL・h5py・
#      webdataset・torchvision・transformers 本体・huggingface_hub・narwhals /
#      cryptography (Google ログイン) / sympy・mpmath・tokenizers (推論チェーンで必要) / pip

