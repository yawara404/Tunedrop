# セキュリティポリシー

> 本ファイルはセルフホスト型Webアプリ用の雛形です。
> `本プロジェクト` をプロジェクト名に読み替えて利用してください。

## サポート対象バージョン

本プロジェクトは GitHub Release でバージョンを区切っています。
サポート対象は **最新のリリースと `main` ブランチの最新コミット** です。

| 対象 | サポート状況 |
| --- | --- |
| 最新のリリース（現在 `v0.1.0`〜） | :white_check_mark: サポート対象 |
| `main` ブランチの最新コミット | :white_check_mark: サポート対象 |
| 過去のリリース・タグ・スナップショット | :x: 対象外 |

## 脆弱性の報告方法

脆弱性を発見した場合は、**公開のIssueには詳細を書かないでください。**

1. GitHubリポジトリの **Security タブ → Report a vulnerability**
   （プライベート脆弱性報告）から報告するのが推奨です。
2. 機能しない場合は、リポジトリオーナーへ GitHub 経由で直接ご連絡ください。

### 報告に含める情報

- 問題の概要と、再現手順（または概念実証コード）
- 影響を受けるファイル・エンドポイント
- 想定される影響度

### 報告に含めない情報

- 本物の秘密情報（APIキー・署名鍵・トークン類）は貼らないでください。
  該当する鍵は報告前に失効・再発行してください。

### 対応の方針

- 受領は **2営業日以内** を目安にご連絡します
- 修正は非公開の環境で準備し、**修正版を `main` へ反映した後**に内容を開示します
- 重要度の判断・修正の可否は、報告内容を検証した上で決定します

## 対象範囲

- 対象: 本リポジトリのコード（サーバーサイド / フロントエンド / 設定ファイル）
- 対象外: 依存する外部サービス自体の問題
  （動画配信・ID基盤・AI API など。各プロバイダの報告窓口をご利用ください）
- 対象外: `.gitignore` で管理外としているローカル専用ファイル
  （データベース・秘密鍵・バックアップなど。運用者側の管理責任です）

## セルフホスト運用者へ

本プロジェクトは自己のサーバー上で運用することを想定しています。
公開サーバーとして運用する場合、以下は運用者ご自身の責任で十分に行ってください。

- HTTPSでの公開（リバースプロキシ・トンネルの利用など）
- 秘密情報の管理（`.env`・署名鍵・管理者トークン類をリポジトリに含めない）
- 公開ディレクトリの制限（DB・鍵・ソース・バックアップをHTTP配信しない）
- 依存パッケージとサーバーOSの更新
